# This project uses Firebase Hosting, not Netlify
# Deploy to Firebase instead using: firebase deploy --only hosting
# 
# To build locally:
# cp common/src/other/AccessKey.example.js common/src/other/AccessKey.js
# cd web-app && npm install --legacy-peer-deps && npm run build


